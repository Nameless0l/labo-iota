export default function ProjectPage({ params }) {
    const { slug } = params;
    // Récupérer les données du projet basé sur le slug
    return (
      <div className="container mx-auto py-16 px-4">
        {/* Contenu du projet */}
      </div>
    );
  }